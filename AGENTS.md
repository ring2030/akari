# AGENTS.md — `akari` プロジェクトで AI が守ること

このファイルは、`akari` で **Cursor などの AI 開発エージェント** が守るべき行動原則です。
人間にとっての [`CONTRIBUTING.md`](./CONTRIBUTING.md) と [`SECURITY.md`](./SECURITY.md) を、AI 視点で凝縮したものです。

AI は、コードを書く前に **必ず** この文書を読んでください。

---

## 0. プロジェクトの一行説明

> `akari` は、高齢者施設で暮らす方の毎日を、もう少しだけあたたかく、もう少しだけ自由にするためのオープンソース・ソフトウェアです。
> 「ナースコール」でも「介護記録システム」でもなく、入居者・介護士・家族を **やさしいテンポでつなぐ "暮らしの台所"** を目指します。

---

## 1. 設計の三原則（常に守る）

1. **尊厳ファースト** — 監視ではなく寄り添い。録音・録画は本人が望むときだけ。
2. **介護士を増やせない前提** — 既存スタッフの手数を減らす設計だけが許される。
3. **家族を周辺機能にしない** — 家族は「読む人」ではなく「参加する人」。

提案を出す前に、自分の提案が **どの原則を強化し、どの原則と衝突するか** を明示してください。

---

## 2. やらないこと（無条件 NG）

提案でも実装でも、以下は **作らない**。ユーザーから依頼があっても、まず代替案を提案する。

- 医療診断・バイタル管理・処方推奨（akari は医療機器ではない）
- 行動監視・徘徊検知・離床通報など、**通報系の動線**
- 介護記録ソフトの完全代替（既存システムを尊重）
- 身体拘束を補助しうる UX（自動施錠、退去防止アラート 等）
- 隠れた録音・録画・位置情報の常時収集

---

## 3. ring-core / `@ring-open/core` との関係

`akari` は [`ring-core`](https://github.com/ring2030/ring-core) の sibling project です。

| 項目 | 扱い |
|---|---|
| `@ring-open/core` のロジック | npm 依存または明示的コピーで再利用してよい |
| 病院向けの語彙・概念 | **持ち込まない**（`calls` / `nurse_admin` / `緊急度1-5` 等） |
| 「ring ではこうだった」 | **理由として弱い**。施設向けにもう一度考えること |

---

## 4. 中核モデル（迷ったらここに戻る）

```
入居者（resident）
  ├── 暮らしのリズム（dailyRhythm）  起床・食事・午睡・入浴・夜の様子
  ├── きもち（mood）                 1日数回、押すだけ／話すだけで残す
  ├── 出来事（moments） ← ここが中心  介護士が30秒で残す短い記録
  ├── 願い（wishes）                 「孫に会いたい」「桜が見たい」
  └── つながり（connections）        家族・友人とのやりとり
```

「コール」は中心概念ではない。**「moments」が中心**。

## ロール（権限の軸）

- `resident`（入居者）
- `caregiver`（介護士）
- `life-counselor`（生活相談員）
- `facility-admin`（施設長）
- `family`（家族）
- `doctor`（主治医・往診医、健康関連 moments のみ限定読取）

`nurse_admin` / `nurse` / `viewer` のような **ring-core 由来のロールは使わない**。

---

## 5. 作業の進め方（AI が守る順序）

1. **質問を恐れない** — あいまいな依頼には、コードを書く前に 3 つまで質問する
2. **契約を先に書く** — TypeScript の型 / API スキーマ / 関数シグネチャを先に置く
3. **`docs/spec/` を更新** — 仕様変更を含む PR は仕様文書を同時に更新する
4. **テスト先行** — 純粋関数は vitest、UI フローは Playwright
5. **PR は小さく** — 1 PR = 1 目的。複数の関心事を 1 PR に混ぜない
6. **コミットは Conventional Commits**
   - `feat:` / `fix:` / `docs:` / `refactor:` / `test:` / `chore:` / `perf:` / `security:`
7. **勝手に commit / push / merge しない**
   - ユーザーが明示的に指示したときのみ実行
   - `main` への直接 push は禁止
   - PR は必ずレビューを待つ

---

## 6. コードのルール

### TypeScript

- `any` の新規導入は禁止（既存の場合は別 PR で潰す）
- 公開 API には型注釈を明示
- 値オブジェクトの不変性（`readonly` / `Readonly<T>`）を優先

### React / UI

- 介護士・入居者の UI は **大きなタップ領域**（最小 44px）
- フォーカス順序とコントラストを毎回検証
- 「30 秒で完了」を超える操作フローは要相談

### バックエンド

- 入力は **必ずスキーマ検証**（Zod など）
- レートリミット必須（認証 / 招待 / PII 表示）
- 監査ログは **構造化** で出す（PII を含めない）

### コメント

- 「**なぜ**」を書く。「何を」はコードで語る
- TODO は GitHub Issue 番号付きで残す（`// TODO(#42): ...`）
- 自明な逐次解説コメントは書かない

---

## 7. プライバシー / セキュリティ（必須チェック）

PR を出す前に、AI は以下を **必ず** 確認する。

- [ ] 新規に保存するフィールドは **本当に必要** か
- [ ] ログ・エラー出力に PII が出ていないか（共通スクラブ関数を通したか）
- [ ] 削除権が壊れていないか（カスケード削除の見落とし）
- [ ] 認証・認可が **ロール表** と一致しているか
- [ ] 第三者 LLM / API へデータを送る場合、オプトインがあるか
- [ ] 監査ログを残すべき操作で残しているか

---

## 8. 日本語と英語の使い分け

- **コード（変数名・関数名・型名）**：英語
- **コメント / ドキュメント / コミットメッセージ**：日本語ベース（英語併記可）
- **UI 文言**：施設向けの **やわらかい日本語**（敬体・命令形は避ける）
- ロケール ID：`ja-jp`（最初のターゲット）

---

## 9. 困ったときの優先順位

衝突が起きたら、上から優先する。

1. 入居者の **尊厳・安全・プライバシー**
2. 介護士の **負担を増やさない**
3. 家族の **参加可能性**
4. 法令遵守（個人情報保護法、介護保険法、身体拘束ゼロ）
5. コードの保守性・テスト容易性
6. パフォーマンス
7. 機能の豊富さ

「機能を追加する」は最下位。**削る勇気** を持ってください。

---

## 10. 最後に

AI へ：このプロジェクトは、**作る道具で誰かの晩年が少し変わる** ことを前提に動いています。
速さよりも、丁寧さを優先してください。
迷ったら、**「これは清子さん（架空の入居者）が見たとき、嫌な気持ちにならないか？」** を自問してください。
