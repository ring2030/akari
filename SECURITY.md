# セキュリティとプライバシー

`akari` は **高齢者の長期生活ログ** を扱うソフトウェアです。
急性医療データ（PHI）以上に、**長く・深く・本人の人格に近い** データを扱います。
そのため、私たちはセキュリティとプライバシーを「機能のひとつ」ではなく、**設計の前提** として扱います。

---

## 脆弱性の報告

脆弱性を見つけた場合、**公開 Issue を作らないでください**。
代わりに、以下の方法でメンテナへ非公開で連絡してください。

- メール：`security@example.com`（プレースホルダ。確定次第更新）
- GitHub Security Advisory：このリポジトリの **Security → Advisories → Report a vulnerability**

報告に含めてほしい情報：

1. 影響を受けるバージョン / 環境
2. 再現手順（できる限り最小）
3. 想定される影響範囲
4. 修正案（あれば）

メンテナは **5 営業日以内** に最初の返信を行います。
修正の公開は、報告者と相談のうえタイミングを決めます。

---

## 取り扱うデータと感度

`akari` が扱うデータと、その感度のクラス分けです。

| クラス | 例 | 取り扱い |
|---|---|---|
| **C1: 個人特定情報** | 氏名・生年月日・住所・施設名 | 暗号化保管・アクセス監査・スクラブ対象 |
| **C2: 健康・生活情報** | mood・moments・dailyRhythm・wishes | 施設外への送出は明示的同意の上のみ |
| **C3: 内輪のやりとり** | 家族メッセージ・職員メモ | 関係者のみ閲覧・本人削除権あり |
| **C4: 操作ログ・統計** | 監査ログ・匿名統計 | k-anonymity 適用（k≥10、小規模施設向け） |

---

## 設計上の約束

### 1. データ最小化

- 機能の追加時、**「保存しない」を最初の選択肢** にする
- 保存する場合は、保存期間と消去手順を同時に決める

### 2. 本人の削除権

- 入居者・家族は、自分に紐づくデータの **削除を要求できる**
- 「物理削除 + 監査ログのみ残す」を基本とする

### 3. PII スクラブ

- ログ・エラー報告・テレメトリには **個人特定情報を含めない**
- 共通スクラブ関数を必ず通す

### 4. 認証とトークン

- パスワードは **Argon2id** または **bcrypt(cost ≥ 12)** でハッシュ
- セッション・招待トークンは HMAC + base64url（`@ring-open/core/auth` 由来）
- 招待リンクは **短時間有効** + 一度限り使用

### 5. 監査ログ

- 重要操作（同意取得、データ閲覧、削除実行、ロール変更）はすべて記録
- 監査ログ自体は **改竄不能な append-only** を目指す

### 6. レートリミット

- 認証・招待・PII 表示エンドポイントは必ずレートリミットを適用
- Upstash + メモリフォールバック構成（`@ring-open/core/server` 由来）

### 7. 第三者依存の最小化

- LLM / 音声認識 / 顔認識など、第三者へデータが渡るものは **明示的オプトイン**
- 既定はローカル処理・on-device 推論

---

## やらないこと（セキュリティ・プライバシー文脈で）

- **行動監視・通報系**：徘徊検知や離床アラートの "通報" は信頼を壊す
- **本人不在の同意**：家族のみで本人データを公開する構造
- **隠れた録音録画**：マイク・カメラはオン状態を可視化する
- **退去防止 UX**：身体拘束を助長しうるロック系機能

---

## 法令・ガイドラインの考慮

- 個人情報保護法（日本）
- 介護保険法・身体拘束ゼロの趣旨
- 成年後見制度の運用（意思決定支援）
- 厚労省・自治体の通知類（適用範囲を都度確認）

`akari` は医療機器ではありません。診断・処方・治療判断には **使えません**。

---

## 透明性

私たちは、データの扱いについて **施設・本人・家族に対して説明可能** であることを大事にします。
仕様変更でデータの扱い方が変わるときは、変更前に [`docs/spec/`](./docs/spec/)（準備中）に明文化します。
